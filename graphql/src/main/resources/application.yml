graphql:
  endpoint: api
  packages:
    - "de.isweariwillhaveabackend"
  subscriptions:
    # Send a ka message every 1000 ms (1 second)
    keepAliveInterval: 1000
spring:
  neo4j:
    uri: "${DB_URL}"
logging:
  level:
    org.springframework.data.neo4j: DEBUG
    io.github.graphglue: DEBUG
